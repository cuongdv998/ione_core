/**
 * Local models for claim task detail page (xử lý bồi thường - xem chi tiết).
 * DTOs from proxy: ClaimDetailDto, ClaimFolderListDto, ClaimStageProgressDto, ClaimSentMessageDto, ClaimDocumentDto, ClaimPolicyDto.
 */

export interface ClaimTaskDetailTabIndex {
  general: number;
  images: number;
  progress: number;
}
